{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    <div>
    {{ form_errors(form) }}
    </div>
    <div class="row">
        <div class="col">
            {{ form_row(form.name) }}
        </div>
    </div>

 <div>
    <button type="submit">Add category</button>
 </div>
    {{ form_end(form) }}
{% endblock %}
